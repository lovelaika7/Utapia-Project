import { Song, Genre, ArtistMeta } from './types';

// Deprecated: Using Google Sheet as Database now.
// Kept for type reference or fallback if needed, but app uses DataContext.

export const MOCK_SONGS: Song[] = [];
export const ARTIST_META: Record<string, ArtistMeta> = {};
